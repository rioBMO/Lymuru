# Environment
.env
.env.docker

# Telethon session (kept in data/, but ignore in case it lives at root)
*.session

# Data (SQLite DB) and runtime artifacts
data/*.db
data/*.db-wal
data/*.db-shm
downloads/
uploads/

# Frontend (React)
node_modules/

# Wails generated bindings (auto-regenerated on every build)
frontend/wailsjs/

# Wails build artifacts
build/
bin/
*.exe
*.app
*.dmg

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Virtual environment (legacy — was for the old FastAPI backend)
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db
.DS_Store

# Misc
.cache
